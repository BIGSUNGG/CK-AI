# 만화 뷰어 기획서 (SPEC)

세로 비율 전용 만화 뷰어. 스크롤 없이 **우→좌로 드래그하면 종이가 말리며 넘어가는 페이지 컬**으로 다음 컷을 본다.
GitHub Pages 정적 배포를 전제로 하며, React 19 + Vite 7 + TypeScript로 구현하고 플립 연출은 motion(motion/react)으로 처리한다.

## 1. 목표 UX

- 책은 총 **22페이지** (컷 이미지 20 + 장면 영상 2).
- 첫 페이지는 오프닝 영상, 3컷과 4컷 사이에 중간 영상이 삽입된다.
- 독자는 화면을 **우→좌로 드래그(스와이프)** 해 다음 페이지로, **좌→우로 드래그**해 이전 페이지로 넘긴다.
- 넘김 연출은 **휘어지는 종이 컬(corner가 아닌 곡면 컬)** — 페이지가 우측 가장자리에서 들려 가상 원통에 감기며 굴러가고(접촉점이 우→좌 이동, 컬 반지름은 넘김 중반에 크고 양끝에서 조임), 말린 부분의 뒷면은 반사된 콘텐츠로 보인다. 컬 곡면에는 진행률 비례 음영·접힘 하이라이트가, 아래 페이지에는 롤 그림자가 따라온다.
- 드래그가 임계값(스테이지 너비의 30%) 미만이거나 속도가 느리면 페이지가 스프링처럼 원래 위치로 돌아간다.
- 마지막 페이지(19.끝)에서 "다시 보기" 버튼으로 처음으로 돌아간다.
- 영상 페이지도 동일하게 플립으로 넘길 수 있으며, 페이지가 활성화되면 재생을 시작한다.

## 2. 페이지 구성 (순서 확정)

| # | 파일 | 원본 | 내용 |
| --- | ------ | ------ | ------ |
| 1 | assets/opening.mp4 | 0-1.시작 전 오프닝.mp4 | 오프닝 영상 |
| 2 | assets/cut-00.png | 0.시작화면.png | 시작화면 |
| 3 | assets/cut-01.png | 1.청이강이가 걷다가.png | |
| 4 | assets/cut-02.png | 2.레시피를 발견.png | |
| 5 | assets/cut-03.png | 3.레시피를 들고 집으로 돌아가서.png | |
| 6 | assets/interlude.mp4 | 3-1.집으로 달려가는 장면.mp4 | 중간 영상 |
| 7~22 | assets/cut-04.png ~ cut-19.png | 4.*.png ~ 19.끝.png | 본편 컷 |

- 원본이 0~19 번호 이미지 20개 + 영상 2개이므로 총 22페이지.
- `non recipe.png`, `캐릭터 컨셉 아트.png`는 작업 자료로 제외한다.

## 3. 상호작용 명세

| 입력 | 동작 |
| ------ | ------ |
| 우→좌 드래그/스와이프 | 다음 페이지 컬 (드래그 거리에 비례해 실시간 넘김) |
| 좌→우 드래그/스와이프 | 이전 페이지 컬 (말렸던 페이지가 되돌아옴) |
| 빠른 플릭(속도 ≥ 0.5px/ms) | 짧은 드래그로도 넘김 확정 |
| 마우스 휠 | 아래/위 방향 이산 페이지 넘김 (400ms 스로틀) |
| 키보드 ↓ / Space / PageDown | 다음 페이지 |
| 키보드 ↑ / PageUp | 이전 페이지 |
| 다시 보기 버튼 | 마지막 페이지에서 첫 페이지로 |

- 넘김 확정 임계값: 드래그 거리 ≥ 스테이지 너비 × 0.3 **또는** 플릭 속도 ≥ 0.5px/ms.
- 컬 기하(src/curl.ts, 원통 롤링 모델): 진행률 q에 따라 접촉점 `xc = W − q·W·(1+OVERSHOOT)`이 우→좌 이동하고 컬 반지름 `r = W·(0.10 + 0.20·sin(πq))`로 변한다. 재료 좌표 m이 `xc`를 지나면 감김 각도 `θ = (m−xc)/r`, 화면 위치 `x = xc + r·sinθ`의 곡면에 배치해 스트립으로 렌더링한다. θ ∈ (0,π)는 앞면(접촉점 오른쪽의 솟는 컬), θ ∈ (π,2π)는 뒷면(좌우 반사 콘텐츠, 평평한 남은 부분 위를 쓰는 꼬리). 감김은 2π에서 capped.
- 렌더링: 넘김 중 페이지는 캔버스(drawImage 스트립)로 그리고, DOM 페이지는 영상 프레임 유지를 위해 숨김 마운트한다. 스트립별 휘어짐 음영 + θ≈π 접힘 하이라이트 + 접촉점 좌우 롤 그림자로 깊이감을 준다.

## 4. 레이아웃 (세로 비율 전용)

- 스테이지는 원본 컷 비율 **941:1672** 고정 (`aspect-ratio`).
- `height: min(100dvh, 100vw × 1672/941)` 로 화면이 가로로 넓어도 항상 **중앙 정렬된 세로 프레임**(레터박스)으로 표시.
- 배경은 따뜻한 어두운 나무 책상 그라데이션(radial)으로 책에 집중.
- 스테이지는 책 형태: 종이색(#f6f1e7) 바탕, 좌측 책등 홈(gutter) 그림자, 우하단 아래 장 스택 그림자, 부드러운 외곽 그림자.
- HUD: 상단에 `n / 22` 진행 표시 + 얇은 진행 바(황동색 #caa96a).

## 5. 영상 처리

- `<video playsinline preload="auto">`, 자체 컨트롤 미사용.
- 페이지가 활성화되면 `play()` 시도 → 브라우저가 음소거 없이 재생을 거부하면 음소거 재생으로 폴백하고 음소거 해제 버튼을 노출.
- 영상 페이지 위에는 드래그가 가능하도록 영상 엘리먼트에 `pointer-events: none`을 주고, 음소거 토글 버튼만 오버레이로 둔다.
- 페이지에서 벗어나면 일시정지하고 처음으로 되감는다.

## 6. 기술 결정

| 항목 | 결정 | 이유 |
| ------ | ------ | ------ |
| 스택 | React 19 + Vite 7 + TypeScript (`src/App.tsx`) | 컴포넌트 구조 + 타입 안정성, Pages용 정적 빌드 |
| 애니메이션 | motion (motion/react) | 드래그 진행률 모션 값 + 스프링 물리; 컬 곡면은 src/curl.ts가 프레임별 캔버스 렌더링 |
| 상태 | `index` + `phase`(idle/forward/backward) 상태 2개 + 모션 값 | 22페이지 고정 순서라 복잡한 상태 관리 불필요 |
| 에셋 | 원본 그대로 복사 (압축·리사이즈 없음) | 공모전 원본 품질 유지 |
| 경로 | 전부 상대 경로 | Pages 프로젝트 사이트 서브경로(`/repo-name/`) 호환 |
| 접근성 | `prefers-reduced-motion` 시 전환 시간 최소화, 버튼 라벨 | 기본 접근성 유지 |

## 7. 배포 계획

1. `.github/workflows/deploy.yml` — push 시 `npm ci && npm run build` 후 빌드 산출물(`dist/`)을 GitHub Pages에 배포 (setup-node + actions/upload-pages-artifact + deploy-pages, 전 action SHA 고정).
2. 사용자는 저장소 생성·푸시 후, 저장소 Settings → Pages → Source를 **GitHub Actions**로 지정하면 라이브 반영된다 (README에 절차 명시).

## 8. 범위 외 (Out of Scope)

- 스크롤 기반 뷰어, 가로 모드, 줌/확대, 목차·썸네일, 여러 작품 지원.
- 이미지 재압축·최적화, 백엔드, PWA.
- 실제 GitHub 푸시/Pages 활성화 (사용자가 직접 수행).
